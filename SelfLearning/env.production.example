# ============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file: cp env.production.example .env
# 2. Generate secure passwords and API keys
# 3. Fill in all values below
# 4. Secure the file: chmod 600 .env
#
# ============================================================================

# MongoDB Configuration
MONGODB_URL=mongodb://mongodb:27017
MONGODB_USERNAME=admin
MONGODB_PASSWORD=CHANGE_THIS_GENERATE_SECURE_PASSWORD_MIN_32_CHARS
DATABASE_NAME=blog_qa_db

# PostgreSQL Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=blog_qa_publishers
POSTGRES_USER=blogqa_admin
POSTGRES_PASSWORD=CHANGE_THIS_GENERATE_SECURE_PASSWORD_MIN_32_CHARS

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-YOUR_ACTUAL_OPENAI_API_KEY_HERE
OPENAI_MODEL=gpt-4o-mini

# API Service Configuration
API_SERVICE_PORT=8005
ADMIN_API_KEY=CHANGE_THIS_GENERATE_SECURE_ADMIN_KEY_MIN_32_CHARS

# Worker Service Configuration
POLL_INTERVAL_SECONDS=5
CONCURRENT_JOBS=1

# CORS Configuration (comma-separated, no spaces)
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Logging Configuration
LOG_LEVEL=INFO

# ============================================================================
# PASSWORD GENERATION (run these commands to generate secure passwords):
# ============================================================================
# MongoDB Password:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
# PostgreSQL Password:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Admin API Key:
#   python3 -c "import secrets; print('admin_' + secrets.token_urlsafe(32))"
# ============================================================================

