global:
  resolve_timeout: 5m
  
  # SMTP Configuration
  # For Gmail, use these settings:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'dev@adtechgrowthpartners.com'  # Change to your email address
  smtp_auth_username: 'dev@adtechgrowthpartners.com'  # Change to your Gmail
  smtp_auth_password: 'kbch gzzr csxb jeik'  # Use Gmail App Password (not regular password)
  smtp_require_tls: true
  
  # For other SMTP providers, adjust accordingly:
  # smtp_smarthost: 'smtp.example.com:587'
  # smtp_from: 'dev@adtechgrowthpartners.com'
  # smtp_auth_username: 'your-username'
  # smtp_auth_password: 'your-password'
  
  # Slack (optional - uncomment if you want Slack too)
  # slack_api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'

# Templates for alert notifications
templates:
  - '/etc/alertmanager/*.tmpl'

# Route tree for alerts
route:
  receiver: 'default-receiver'
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  routes:
    # Critical alerts route immediately to critical-receiver
    - match:
        severity: critical
      receiver: 'critical-receiver'
      continue: true
      group_wait: 0s  # Send immediately
    
    # Warning alerts
    - match:
        severity: warning
      receiver: 'warning-receiver'
      continue: true

# Inhibit rules (suppress certain alerts when others are firing)
inhibit_rules:
  # Don't alert about high connections if DB is down
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']

# Receivers
receivers:
  # Default receiver (for unmatched alerts)
  - name: 'default-receiver'
    email_configs:
      - to: 'dev@adtechgrowthpartners.com'  # Change to your email
        headers:
          Subject: '⚠️ Alert: {{ .GroupLabels.alertname }}'
        html: |
          <h2>Alert Details</h2>
          <table border="1" cellpadding="5" cellspacing="0">
            <tr><td><strong>Alert:</strong></td><td>{{ .GroupLabels.alertname }}</td></tr>
            <tr><td><strong>Severity:</strong></td><td>{{ .CommonLabels.severity }}</td></tr>
            <tr><td><strong>Service:</strong></td><td>{{ .CommonLabels.service }}</td></tr>
            <tr><td><strong>Status:</strong></td><td>{{ .Status }}</td></tr>
            <tr><td><strong>Started:</strong></td><td>{{ .StartsAt }}</td></tr>
          </table>
          <h3>Description</h3>
          <p>{{ range .Alerts }}{{ .Annotations.description }}{{ end }}</p>
          <h3>Labels</h3>
          <ul>
            {{ range .CommonLabels.SortedPairs }}
            <li><strong>{{ .Name }}:</strong> {{ .Value }}</li>
            {{ end }}
          </ul>
        send_resolved: true
  
  # Critical alerts receiver
  - name: 'critical-receiver'
    email_configs:
      - to: 'dev@adtechgrowthpartners.com'  # Change to your email
        headers:
          Subject: '🚨 CRITICAL: {{ .GroupLabels.alertname }}'
        html: |
          <h2 style="color: red;">🚨 CRITICAL ALERT</h2>
          <table border="1" cellpadding="5" cellspacing="0" style="border-collapse: collapse;">
            <tr style="background-color: #ffcccc;">
              <td><strong>Alert:</strong></td>
              <td><strong>{{ .GroupLabels.alertname }}</strong></td>
            </tr>
            <tr>
              <td><strong>Severity:</strong></td>
              <td style="color: red;"><strong>CRITICAL</strong></td>
            </tr>
            <tr>
              <td><strong>Service:</strong></td>
              <td>{{ .CommonLabels.service }}</td>
            </tr>
            <tr>
              <td><strong>Status:</strong></td>
              <td>{{ .Status }}</td>
            </tr>
            <tr>
              <td><strong>Started:</strong></td>
              <td>{{ .StartsAt }}</td>
            </tr>
          </table>
          <h3>Description</h3>
          <p style="font-size: 14px;">{{ range .Alerts }}{{ .Annotations.description }}{{ end }}</p>
          <h3>Summary</h3>
          <p style="font-size: 14px;">{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}</p>
          <h3>Labels</h3>
          <ul>
            {{ range .CommonLabels.SortedPairs }}
            <li><strong>{{ .Name }}:</strong> {{ .Value }}</li>
            {{ end }}
          </ul>
          <hr>
          <p style="color: #666; font-size: 12px;">
            This is a critical alert requiring immediate attention.
          </p>
        send_resolved: true
  
  # Warning alerts receiver
  - name: 'warning-receiver'
    email_configs:
      - to: 'dev@adtechgrowthpartners.com'  # Change to your email
        headers:
          Subject: '⚠️ Warning: {{ .GroupLabels.alertname }}'
        html: |
          <h2 style="color: orange;">⚠️ Warning Alert</h2>
          <table border="1" cellpadding="5" cellspacing="0" style="border-collapse: collapse;">
            <tr style="background-color: #fff4cc;">
              <td><strong>Alert:</strong></td>
              <td><strong>{{ .GroupLabels.alertname }}</strong></td>
            </tr>
            <tr>
              <td><strong>Severity:</strong></td>
              <td style="color: orange;"><strong>WARNING</strong></td>
            </tr>
            <tr>
              <td><strong>Service:</strong></td>
              <td>{{ .CommonLabels.service }}</td>
            </tr>
            <tr>
              <td><strong>Status:</strong></td>
              <td>{{ .Status }}</td>
            </tr>
            <tr>
              <td><strong>Started:</strong></td>
              <td>{{ .StartsAt }}</td>
            </tr>
          </table>
          <h3>Description</h3>
          <p style="font-size: 14px;">{{ range .Alerts }}{{ .Annotations.description }}{{ end }}</p>
          <h3>Summary</h3>
          <p style="font-size: 14px;">{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}</p>
          <h3>Labels</h3>
          <ul>
            {{ range .CommonLabels.SortedPairs }}
            <li><strong>{{ .Name }}:</strong> {{ .Value }}</li>
            {{ end }}
          </ul>
          <hr>
          <p style="color: #666; font-size: 12px;">
            This is a warning alert. Please review and take action if necessary.
          </p>
        send_resolved: true
